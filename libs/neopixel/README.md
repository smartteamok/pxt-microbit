# NeoPixel

Minimal local NeoPixel driver used by bundled SmartTeam blocks.

This package intentionally exposes only the API surface required by SmartTeam outputs and sends data through the micro:bit target's native WS2812 buffer support.
